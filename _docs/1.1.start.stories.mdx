import { Meta } from '@storybook/blocks'

<Meta title="General/Начало работы" parameters={{ previewTabs: { canvas: { hidden: true } } }} id="General/start" />

# UI-KIT библиотека для разработки на React

# Установка

Установить всю библиотеку:

```bash
yarn add @eubank/kit-ui
```

Каждый компонент публикуется отдельным пакетом, поэтому вы можете подключить только нужный, не устанавливая библиотеку целиком:

```bash
yarn add @eubank/kit-button
```

## Использование

```jsx
import { Button } from '@eubank/kit-button'
```
