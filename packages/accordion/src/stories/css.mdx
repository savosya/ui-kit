```css
        :root {
        --accordion-divider-color: var(--bg-primary);
        --accordion-color_extra: #414141;

        --accrodion-bg: #FFF;
        --accrodion-bg_disabled: var(--bg-info);
    }
```