import { Canvas } from '@storybook/blocks'
import * as AccordionStories from './Component.stories'

### Стандартный Accordion

<Canvas of={AccordionStories.Sandbox} />

### Controlled Accordion

Управлять состоянием компонента можно явно передавая props open.

<Canvas of={AccordionStories.Controlled} />

### Accordion с динамическим контентом

<Canvas of={AccordionStories.Dynamic} />
