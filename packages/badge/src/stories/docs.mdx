import { Meta, Controls, Canvas } from '@storybook/blocks'
import { ComponentHeader, Tabs, stickyCanvasObj } from '../../../../.storybook/components'
import packageJson from '../../package.json'
import { Badge } from '../Component'
import Description from './description.mdx'
import CSS from './css.mdx'
import * as BadgeStories from './Component.stories'

<Meta title="Components/Badge" component={Badge} id="Badge" />

<ComponentHeader name="Badge" version={packageJson.version} package="@eubank/kit-badge">
  Компонент бейджа
</ComponentHeader>

```jsx
import { Badge } from '@eubank/kit-root'
import { Badge } from '@eubank/kit-badge'
```

<Canvas of={BadgeStories.Sandbox} meta={BadgeStories} {...stickyCanvasObj} />

<Tabs docs={<Description />} props={<Controls of={BadgeStories.Sandbox} />} css={<CSS />} />
