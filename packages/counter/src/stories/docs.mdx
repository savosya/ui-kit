import { Meta, Controls, Canvas } from '@storybook/blocks'
import { ComponentHeader, Tabs, stickyCanvasObj } from '../../../../.storybook/components'
import packageJson from '../../package.json'
import { Counter } from '../Component'
import Description from './description.mdx'
import * as CounterStories from './Component.stories'

<Meta title="Components/Counter" component={Counter} id="Counter" />

<ComponentHeader name="Counter" version={packageJson.version} package="@eubank/kit-counter">
  Компонент счетчика
</ComponentHeader>

```jsx
import { Counter } from '@eubank/kit-root'
import { Counter } from '@eubank/kit-counter'
```

<Canvas of={CounterStories.Sandbox} meta={CounterStories} {...stickyCanvasObj} />

<Tabs docs={<Description />} props={<Controls of={CounterStories.Sandbox} />} />
