import { Meta, Controls, Canvas } from '@storybook/blocks'
import { ComponentHeader, Tabs, stickyCanvasObj } from '../../../../.storybook/components'
import packageJson from '../../package.json'
import { Tooltip } from '../Component'
import CSS from './css.mdx'
import Description from './description.mdx'
import * as TooltipStories from './Component.stories'

<Meta title="Components/Tooltip" component={Tooltip} id="Tooltip" />

<ComponentHeader name="Tooltip" version={packageJson.version} package="@eubank/kit-tooltip">
  Тултип
</ComponentHeader>

```jsx
import { Tooltip } from '@eubank/kit-root'
import { Tooltip } from '@eubank/kit-tooltip'
```

<Canvas of={TooltipStories.Sandbox} meta={TooltipStories} {...stickyCanvasObj} />

<Tabs docs={<Description />} props={<Controls of={TooltipStories.Sandbox} />} css={<CSS />} />
