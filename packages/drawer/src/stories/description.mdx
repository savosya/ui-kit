import { Canvas } from '@storybook/blocks';
import * as DrawerStories from './Component.stories';

#### Т.к. открытие панели происходит за пределами компонента, в обязательном порядке нужно прокинуть пропсы open и onClose.

<br />
<br />

### Кастомизация панели

<Canvas of={DrawerStories.Custom} meta={DrawerStories} />
