import { Meta, Controls, Canvas } from '@storybook/blocks'
import { ComponentHeader, Tabs, stickyCanvasObj } from '../../../../.storybook/components'
import packageJson from '../../package.json'
import { Loader } from '../Component'
import Description from './description.mdx'
import * as LoaderStories from './Component.stories'

<Meta title="Components/Loader" component={Loader} id="Loader" />

<ComponentHeader name="Loader" version={packageJson.version} package="@eubank/kit-loader">
  Индикатор загрузки
</ComponentHeader>

```jsx
import { Loader } from '@eubank/kit-ui'
import { Loader } from '@eubank/kit-loader'
```

<Canvas of={LoaderStories.Sandbox} meta={LoaderStories} {...stickyCanvasObj} />

<Tabs docs={<Description />} props={<Controls of={LoaderStories.Sandbox} />} />
