import { Canvas } from '@storybook/blocks'
import * as LoaderStories from './Component.stories'

### Отцентрированный loader

Стандартное состояния загрузчика - занимать центральную позицию блока в которой он был размещен

<Canvas of={LoaderStories.NotFilled} meta={LoaderStories} />
