import { Meta, Controls, Canvas } from '@storybook/blocks'
import { ComponentHeader, Tabs, stickyCanvasObj } from '../../../../.storybook/components'
import packageJson from '../../package.json'
import { Button } from '../Component'
import Description from './description.mdx'
import CSS from './css.mdx'
import * as ButtonStories from './Component.stories'

<Meta title="Components/Button" component={Button} id="Button" />

<ComponentHeader name="Button" version={packageJson.version} package="@eubank/kit-button">
  Компонент кнопки
</ComponentHeader>

```jsx
import { Button } from '@eubank/kit-root'
import { Button } from '@eubank/kit-button'
```

<Canvas of={ButtonStories.Sandbox} meta={ButtonStories} {...stickyCanvasObj} />

<Tabs docs={<Description />} props={<Controls of={ButtonStories.Sandbox} />} css={<CSS />} />
