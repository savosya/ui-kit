import { Meta, Story, ArgsTable, Canvas } from '@storybook/blocks';
import { text, select, boolean, number } from '@storybook/addon-knobs';
import { Attach } from '../Component';
import packageJson from '../../package.json';
import Description from './description.mdx';
import {ComponentHeader, Tabs, CssVars} from "../../../../.storybook/components";

<Meta title='Components/Attach' component={Attach} id='Attach' />

<ComponentHeader
    name='Attach'
    version={packageJson.version}
    package='@savosya/savosya-myuikit-attach'
/>

```jsx
import { Attach } from '@savosya/savosya-myuikit-attach';

import { Attach } from '@savosya/savosya-myuikit-root';
```

<Tabs
    docs={<Description/>}
    props={<ArgsTable of={Attach} />}
    css={<CssVars css={`
        :root {
        --accordion-divider-color: var(--bg-primary);
        --accordion-color_extra: #414141;

        --accrodion-bg: #FFF;
        --accrodion-bg_disabled: var(--bg-info);
    }
`}/>}
/>



<Story name='Attach'>
    <Attach
        size={select('size', ['xxs', 'xs', 's', 'm', 'l'], 's')}
        buttonContent={text('buttonContent', undefined)}
        disabled={boolean('disabled', false)}
        maxFilenameLength={number('maxFilenameLength', undefined)}
        multiple={boolean('multiple', false)}
        noFileText={text('noFileText', undefined)}
        progressBarPercent={number('progressBarPercent', undefined)}
    />
</Story>
