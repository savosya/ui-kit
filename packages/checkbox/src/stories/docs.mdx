import { Meta, Controls, Canvas } from '@storybook/blocks'
import { ComponentHeader, Tabs, stickyCanvasObj } from '../../../../.storybook/components'
import packageJson from '../../package.json'
import { Checkbox } from '../Component'
import Description from './description.mdx'
import CSS from './css.mdx'
import * as CheckboxStories from './Component.stories'

<Meta title="Components/Checkbox" component={Checkbox} id="Checkbox" />

<ComponentHeader name="Checkbox" version={packageJson.version} package="@eubank/kit-checkbox">
  Компонент чекбокс
</ComponentHeader>

```jsx
import { Checkbox } from '@eubank/kit-root'
import { Checkbox } from '@eubank/kit-checkbox'
```

<Canvas of={CheckboxStories.Sandbox} meta={CheckboxStories} {...stickyCanvasObj} />

<Tabs docs={<Description />} props={<Controls of={CheckboxStories.Sandbox} />} css={<CSS />} />
