import { Meta, Controls, Canvas } from '@storybook/blocks'
import { ComponentHeader, Tabs, stickyCanvasObj } from '../../../../.storybook/components'
import packageJson from '../../package.json'
import { Divider } from '../Component'
import CSS from './css.mdx'
import * as DividerStories from './Component.stories'

<Meta title="Components/Divider" component={Divider} id="Divider" />

<ComponentHeader name="Divider" version={packageJson.version} package="@eubank/kit-divider">
  Разделитель
</ComponentHeader>

```jsx
import { Divider } from '@eubank/kit-ui'
import { Divider } from '@eubank/kit-divider'
```

<Canvas of={DividerStories.Sandbox} meta={DividerStories} {...stickyCanvasObj} />

<Tabs props={<Controls of={DividerStories.Sandbox} />} css={<CSS />} />
