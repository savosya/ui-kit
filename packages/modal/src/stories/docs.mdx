import { Meta, Controls, Canvas } from '@storybook/blocks'
import { ComponentHeader, Tabs, stickyCanvasObj } from '../../../../.storybook/components'
import packageJson from '../../package.json'
import { Modal } from '../Component'
import Description from './description.mdx'
import * as ModalStories from './Component.stories'

<Meta title="Components/Modal" component={Modal} id="Modal" />

<ComponentHeader name="Modal" version={packageJson.version} package="@eubank/kit-modal">
  Модальное окно
</ComponentHeader>

```jsx
import { Modal } from '@eubank/kit-ui'
import { Modal } from '@eubank/kit-modal'
```

<Canvas of={ModalStories.Sandbox} meta={ModalStories} {...stickyCanvasObj} />

<Tabs docs={<Description />} props={<Controls of={ModalStories.Sandbox} />} />
